<?php $erreur = $erreur ?? null; $mail = $mail ?? ''; $comptesDemo = $comptesDemo ?? []; require __DIR__ . '/../includes/header.php'; ?>
<div class="wrap page-head"><h1>Espace membre</h1><p class="lead">Adhérents, employés et administrateurs utilisent le même formulaire.</p></div>
<div class="wrap section"><div class="form form-etroit"><?php if ($erreur): ?><p class="flash flash-erreur"><?= e($erreur) ?></p><?php endif; ?><form method="post" novalidate><?= champ_csrf() ?><div class="form-ligne"><label for="mail">Adresse e-mail</label><input type="email" id="mail" name="mail" value="<?= e($mail) ?>" required autofocus autocomplete="username"></div><div class="form-ligne"><label for="motdepasse">Mot de passe</label><input type="password" id="motdepasse" name="motdepasse" required autocomplete="current-password"></div><button class="btn btn-vert" type="submit">Se connecter</button></form>
<div class="note" style="margin-top:24px"><b>Comptes de démonstration</b><div style="display:grid;gap:8px;margin-top:10px"><?php foreach ($comptesDemo as $compte): ?><button class="btn btn-ghost btn-sm compte-demo" type="button" data-identifiant="<?= e($compte[0]) ?>" data-motdepasse="apex2026"><?= e($compte[0]) ?> — <?= e($compte[1]) ?></button><?php endforeach; ?></div></div></div></div>
<?php require __DIR__ . '/../includes/footer.php'; ?>
